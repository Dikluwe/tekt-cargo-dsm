# Prompt: armar o V10 — Arena no `[layers]` + `[excluded]` (defesa em profundidade)

**Camada**: config (`crystalline.toml`) — no `tekt-cargo-dsm`.
**Criado em**: 2026-06-07
**Estado**: `EXECUTADO` (laudo `00_nucleo/lessons/0067-armar_v10.md`)
**Pré-requisito**: 0066 (config **(c)** provada: `lab` em `[layers]` **E** `[excluded]`
→ V10 com alvo **e** V1 = 0).
**Decisão do autor**: **armar o V10** (defesa em profundidade).
**Objetivo**: aplicar ao **repo real** a config (c) — a Arena (`lab`) volta ao
`[layers]` (alvo do V10) **mantendo-se** no `[excluded]` (poda do walk, V1 = 0) —
com comentário. Confirmar que o estado limpo do 0065 **não muda** e que a arma **tem
dente** contra a config aplicada. **Só config** — preserva comportamento.

---

## Contexto (do 0066, para não repetir)

O V10 acha a quarentena pelo layer do membro no registry, derivado do `[layers]`. O
0065 tirou o `lab` do `[layers]` (a mais — o `[excluded]` sozinho já dá V1 = 0),
deixando o V10 sem alvo. **No repo real a Arena é workspace separado** → nunca é
membro → o V10 fica vazio **hoje de qualquer forma**. Esta config **arma** o V10 para
o caso **futuro** de alguém ligar um crate da Arena aos membros principais: aí, com o
`[layers]` no lugar, o V10 dispararia.

---

## O que fazer

1. **`crystalline.toml`**: adicionar a Arena de volta ao `[layers]` (o dir `lab/` →
   o layer de quarentena, o mesmo nome de antes do 0065), **mantendo-a** no
   `[excluded]`. Comentário curto explicando a **dupla presença**:
   - em `[layers]` → para o V10 ter o alvo **se** um crate da Arena for ligado aos
     membros principais;
   - em `[excluded]` → para podar do walk (V1 = 0).
2. **Linter completo** e confirmar que o estado limpo do 0065 **segue intacto**:
   **V1 = 0** (o `lab` continua podado pelo `[excluded]`; o `[layers]` não reintroduz
   V1 — provado em (c)), **V3 = 0**, **V12 = 1** (`ErroLente`), **V2 = 1** (`consulta`),
   **demais = 0**.
3. **Reconfirmar a arma contra a config REAL** (em **worktree descartável**, removido
   ao fim): adicionar **um** crate da Arena aos `[workspace].members` + injetar um
   import produção→lab → o **V10 dispara**. Sob a config do 0065 (sem o `[layers]`) a
   **mesma** prova fica em **silêncio**. Isto atesta que a arma tem dente **contra o
   que foi de fato commitado** (não só contra a config de mão do 0066).
4. **Verificar**: suíte **273 + 28** (config não toca código); `cargo build`;
   `prompt/` intocado; o repo real **só** com a mudança de **uma config** (a prova do
   passo 3 vive em worktree, removido).

---

## O que NÃO fazer

- Mudar **código** — só `crystalline.toml`.
- **Adicionar a Arena aos `[workspace].members` do repo REAL** — ela **continua
  workspace separado**; a adição é **só** na prova descartável (worktree).
- Tocar no `prompt/`.
- Mexer no `V12`/`V2` — o V2 do `consulta` é o **próximo** prompt (muda código).

---

## Critérios de Verificação

```
Dado o crystalline.toml
Então lab está em [layers] E em [excluded], com comentário explicando a dupla presença

Dado o linter completo (config real pós-0067)
Então V1 = 0, V3 = 0, V12 = 1, V2 = 1, demais 0 — o estado do 0065 intacto

Dado a prova (worktree descartável): Arena ligada aos membros + leak injetado
Então o V10 dispara sob a config pós-0067 e fica em silêncio sob a do 0065 — a arma
tem dente contra o que foi commitado

Dado a suíte e o repo real
Então 273 + 28; o repo real só com a mudança de config; prompt/ intocado
```

---

## Resultado esperado

- O `crystalline.toml` com a Arena em `[layers]` **e** `[excluded]`, documentado.
- O linter no **mesmo estado limpo** do 0065 (V1 = 0, V3 = 0, V12 = 1, V2 = 1,
  demais 0) — a config não perturbou nada.
- A **prova** (worktree) de que o V10 está armado: dispara no caso futuro (Arena nos
  membros + leak), silêncio sob a config antiga.
- A suíte **273 + 28**.
- **Laudo** em `00_nucleo/lessons/0067-…`: a mudança, a confirmação do estado limpo,
  a prova da arma, e o **único pendente**: o V2 do `consulta` (próximo, muda código,
  encerra a migração).

---

## Cuidados

- **A Arena NÃO entra nos membros do workspace real** — segue workspace separado; a
  adição aos membros é **só** na prova descartável.
- **V1 = 0 tem de seguir** — o `[excluded]` é o que poda; o `lab` no `[layers]` **não**
  traz V1 (provado em (c)). Se V1 subir, algo saiu do esperado — reportar.
- A **reconfirmação contra a config real** fecha o que o 0066 provou contra uma config
  de mão — a arma é atestada **no que foi commitado**.
- **Comportamento idêntico** — só config; suíte 273 + 28; `prompt/` intocado.

---

## Histórico de Revisões

| Data | Motivo | Arquivos afetados |
|------|--------|-------------------|
| 2026-06-07 | Decisão do autor (informada pelo 0066): **armar o V10** com a config (c). `crystalline.toml`: a Arena (`lab`) volta ao `[layers]` (dir `lab/` → layer de quarentena) **mantendo-se** no `[excluded]` — comentário explica a dupla presença (em `[layers]` para o V10 ter alvo se um crate da Arena for ligado aos membros principais; em `[excluded]` para podar do walk → V1 = 0). Confirmado que o estado limpo do 0065 **não muda**: V1 = 0 (o `[excluded]` poda; o `[layers]` não reintroduz V1, provado em (c)), V3 = 0, V12 = 1 (`ErroLente`), V2 = 1 (`consulta`), demais 0. **Reconfirmada a arma contra a config real** (worktree descartável, removido): Arena ligada aos `[workspace].members` + import produção→lab injetado → V10 **dispara**; sob a config do 0065 → **silêncio**. A Arena **não** entra nos membros do repo real (segue workspace separado). **Preserva comportamento**: só config; suíte 273 + 28; `prompt/` intocado. Resta só o teste mínimo do `consulta` (próximo prompt, muda código) para zerar o V2 e encerrar a migração. | `crystalline.toml` (`[layers]` + `[excluded]` da Arena, com comentário), `00_nucleo/lessons/0067-armar_v10.md` |
