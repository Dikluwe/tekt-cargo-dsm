# Laudo de Execução — Prompt 0067 (armar o V10 — Arena em `[layers]` + `[excluded]`)

**Camada**: config (`crystalline.toml`)
**Data**: 2026-06-07
**Prompt executado**: `00_nucleo/prompt/0067-armar_v10.md`
**Estado**: `EXECUTADO` — config (c) aplicada ao repo real. Estado limpo do 0065
**intacto** (V1=0, V3=0, V12=1, V2=1, demais 0). Arma **reconfirmada contra a config
commitada**: V10 dispara sob a config pós-0067, silêncio sob a do 0065. Só config —
suíte 273, 0 falhas.

---

## A resposta em uma sentença

A Arena (`lab`) voltou ao `[layers]` (para o V10 ter alvo se um crate da Arena for
ligado aos membros principais) **mantendo-se** no `[excluded]` (poda do walk → V1=0);
o `[layers]` **não** reintroduziu V1 (o registry lê `[layers]` independente do walk) —
e a prova contra a config **commitada** mostra a arma com dente.

---

## A mudança no `crystalline.toml`

Adicionada a linha `lab = "lab"` ao `[layers]`, com comentário explicando a **dupla
presença** (`[layers]` + `[excluded]`):

- **em `[layers]`** → o `CrateRegistry` deriva o layer do membro daqui; SE um crate da
  Arena for ligado aos `[workspace].members` principais, um import produção→lab resolve
  para `Layer::Lab` e o **V10 dispara** (defesa em profundidade, caso futuro);
- **em `[excluded]`** → poda o `lab/` do walk → os arquivos da Arena não ganham
  cabeçalho de linhagem → **V1 = 0**. O `[layers]` não reintroduz V1 porque o registry
  lê `[layers]` independentemente da poda do walk (provado em (c), laudo 0066).

Nenhuma outra mudança. Código intocado.

---

## Estado limpo do 0065 — intacto (config real pós-0067)

| Check | Projeto | Nota |
|---|---|---|
| **V1** | **0** ✅ | o `[excluded]` segue podando o `lab/`; o `[layers]` não trouxe V1 |
| V3 | 0 | direção preservada |
| **V12** | **1** | `ErroLente` (L4) — intencional, documentado (0065) |
| **V2** | **1** | `consulta.rs` — pré-existente, **próximo prompt** (muda código) |
| demais (V4–V11, V13, V14) | 0 | invariantes limpos |

O `[layers]` da Arena **não perturbou nada** — o marco V1=0 do 0065 segue de pé.

---

## A arma reconfirmada — contra a config COMMITADA (não a de mão do 0066)

Worktree descartável (removido ao fim). Mesma injeção: um crate da Arena
(`proto-impacto-diff`) ligado aos `[workspace].members` principais + import
produção→lab (`use proto_impacto_diff::algo_qualquer;` em `lente_filtro`, L1):

| Config | lab em `[layers]`? | V10 |
|---|---|---|
| **(1)** real pós-0067 (commitada) | sim | **DISPARA** (fatal: "Quarentena violada… não pode importar de lab/") |
| **(2)** do 0065 | não | **silêncio** |

A mesma violação dispara sob a config commitada e cala sob a antiga → a arma tem
**dente contra o que foi de fato aplicado**, não só contra a config de mão do 0066.
Diferença deste laudo p/ o 0066: lá a config (1) era montada à mão no worktree; aqui é
o `crystalline.toml` **real** copiado para o worktree — atesta o commit.

---

## A ressalva (segue valendo, do 0066)

A Arena **continua workspace separado** no repo real — **não** entra nos
`[workspace].members`. Logo, **hoje**, seus crates nunca são membros do registry e o
V10 fica **sem alvo de fato**. Esta config é a **arma carregada, não disparada**: só
ganha dente real se alguém, no futuro, ligar um crate da Arena aos membros principais
— e aí o V10 acende. A adição aos membros viveu **só** na prova (worktree, removido);
o repo real não a tem.

---

## Verificação de comportamento

| Item | Resultado |
|------|-----------|
| `cargo build --workspace` | passa |
| `cargo test --workspace` | **273 passed, 0 failed** (25 `#[ignore]` — fork/rede, pré-existentes) |
| Linter (config real) | V1=0, V3=0, V12=1, V2=1, demais 0 — estado do 0065 intacto |
| Repo real | só `crystalline.toml` modificado (+ prompt/laudo 0067); `prompt/` intocado; código intocado |

---

## O que resta (encerra a migração)

- **Só o V2 = 1 do `consulta.rs`** — um `#[cfg(test)] mod tests` mínimo (ex.:
  `Escopo::default() == Completo`, `ModoUses::default() == Todas`) zera o último V2.
  **Muda código** → próximo prompt. Depois disso, o linter fica **só com o V12 = 1
  do `ErroLente`** (warning intencional aceito) — a convenção Cristalina plenamente
  adotada, e o V10 **armado** para o futuro.

---

## Histórico de Revisões

| Data | Motivo | Arquivos afetados |
|------|--------|-------------------|
| 2026-06-07 | Decisão do autor (informada pelo 0066): **armar o V10** com a config (c). `crystalline.toml`: a Arena (`lab`) volta ao `[layers]` (`lab = "lab"`) **mantendo-se** no `[excluded]` — comentário explica a dupla presença (`[layers]` → alvo do V10 se um crate da Arena for ligado aos `[workspace].members` principais; `[excluded]` → poda do walk → V1=0; o `[layers]` não reintroduz V1, o registry o lê independente do walk — provado em (c)/0066). **Estado limpo do 0065 intacto**: V1=0, V3=0, V12=1 (`ErroLente`), V2=1 (`consulta`), demais 0. **Arma reconfirmada contra a config COMMITADA** (worktree descartável, removido; `crystalline.toml` real copiado p/ o worktree): um crate da Arena ligado aos membros + import produção→lab → **(1)** config pós-0067 → V10 **dispara** (fatal); **(2)** config do 0065 → **silêncio**. A Arena **não** entra nos membros do repo real (segue workspace separado) — a adição viveu só na prova. **Preserva comportamento**: só config; `cargo test` 273 passed / 0 failed; `prompt/` e código intocados. Resta só o teste mínimo do `consulta` (próximo prompt, muda código) para zerar o V2 e encerrar a migração — com o V10 agora armado. | `crystalline.toml` (`lab` em `[layers]` + `[excluded]`, com comentário), `00_nucleo/lessons/0067-armar_v10.md` |
