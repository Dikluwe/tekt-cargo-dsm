[package]
name = "lente_infra"
version = "0.0.0"
edition = "2024"
rust-version = "1.91"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
lente_core = { path = "../01_core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
